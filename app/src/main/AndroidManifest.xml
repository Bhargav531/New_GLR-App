<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.steveBrains.my_glr">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".battery_steps"></activity>
        <activity android:name=".Wheels_steps" />
        <activity android:name=".priter_problems" />
        <activity android:name=".mobile_problems" />
        <activity android:name=".bike_problems" />
        <activity android:name=".car_problems" />
        <activity android:name=".All_objects" />
        <activity android:name=".Scan" />
        <activity android:name=".Instactions" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>